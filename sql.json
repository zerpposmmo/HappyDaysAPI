{
  "navigate": "SELECT CHEMINPRODUIT.ORDRE, CHEMINPRODUIT.PRODUIT_ID, PRODUIT.POIDS, PRODUIT.VOLUME, LOCALISATION.X, LOCALISATION.Y, CHEMIN.TOURNEE_ID, QTEPRODUITSCOLIS.COLIS_ID, QTEPRODUITSCOLIS.QUANTITE, COLIS.* FROM CHEMINPRODUIT INNER JOIN PRODUIT ON PRODUIT.ID = CHEMINPRODUIT.PRODUIT_ID INNER JOIN LOCALISATION ON LOCALISATION.ID = PRODUIT.LOCALISATION_ID INNER JOIN CHEMIN ON CHEMIN.ID = CHEMINPRODUIT.CHEMIN_ID INNER JOIN QTEPRODUITSCOLIS ON QTEPRODUITSCOLIS.PRODUIT_ID = PRODUIT.ID INNER JOIN COLIS ON QTEPRODUITSCOLIS.COLIS_ID = COLIS.ID WHERE CHEMIN.TOURNEE_ID=",
  "order": "SELECT * FROM COMMANDE INNER JOIN LIGNE ON COMMANDE.ID = LIGNE.COMMANDE_ID INNER JOIN PRODUIT ON LIGNE.PRODUIT_ID = PRODUIT.ID INNER JOIN LOCALISATION ON LOCALISATION.ID = PRODUIT.LOCALISATION_ID WHERE COMMANDE.ID=",
  "orders": "SELECT * FROM COMMANDE",
  "package": "SELECT * FROM QTEPRODUITSCOLIS INNER JOIN PRODUIT ON PRODUIT.ID = QTEPRODUITSCOLIS.ID INNER JOIN LOCALISATION ON LOCALISATION.ID = PRODUIT.LOCALISATION_ID WHERE QTEPRODUITSCOLIS.COLIS_ID=",
  "product": "SELECT * FROM LOCALISATION INNER JOIN PRODUIT ON LOCALISATION.ID = PRODUIT.LOCALISATION_ID WHERE PRODUIT.ID =",
  "products": "SELECT * FROM LOCALISATION INNER JOIN PRODUIT ON LOCALISATION.ID = PRODUIT.LOCALISATION_ID ORDER BY PRODUIT.ID",
  "tour": "SELECT * FROM COLIS WHERE TOURNEE_ID =",
  "tours": "SELECT * FROM TOURNEE"
}