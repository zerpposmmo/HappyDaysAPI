{
  "type": "json",
  "query": "SELECT * FROM TOURNEE INNER JOIN COLIS ON TOURNEE.ID = COLIS.TOURNEE_ID INNER JOIN QTEPRODUITSCOLIS ON COLIS_ID = QTEPRODUITSCOLIS.COLIS_ID INNER JOIN PRODUIT ON QTEPRODUITSCOLIS.PRODUIT_ID = PRODUIT.INSTANCE_ID INNER JOIN LOCALISATION ON LOCALISATION.ID = PRODUIT.LOCALISATION_ID"

}